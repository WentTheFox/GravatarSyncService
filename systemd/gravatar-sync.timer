[Unit]
Description=Periodically sync local account picture from Gravatar

[Timer]
# Run shortly after boot (network-online.target ordering happens in the
# service itself), then once a day thereafter.
OnBootSec=2min
OnUnitActiveSec=1d
Persistent=true

[Install]
WantedBy=timers.target
